//! RealtimeHub 实时事件中心 (Task 7 填充)

use std::sync::Arc;

/// 占位类型,Task 7 替换为完整实现
pub struct RealtimeHub;

impl RealtimeHub {
    pub fn new(_capacity: usize) -> Arc<Self> {
        Arc::new(RealtimeHub)
    }
}
